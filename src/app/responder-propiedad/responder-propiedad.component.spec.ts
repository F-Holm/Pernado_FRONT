/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ResponderPropiedadComponent } from './responder-propiedad.component';

describe('ResponderPropiedadComponent', () => {
  let component: ResponderPropiedadComponent;
  let fixture: ComponentFixture<ResponderPropiedadComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [ResponderPropiedadComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ResponderPropiedadComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
